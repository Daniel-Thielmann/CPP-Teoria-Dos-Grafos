#ifndef N_CONEXO_H
#define N_CONEXO_H

#include <vector>

// Função que retorna o número de componentes conexas de um grafo não direcionado
int n_conexo(const std::vector<std::vector<int>>& adjacencias, int numVertices);

#endif
